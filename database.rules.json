{
    "rules": {
        ".read": "auth != null",
        ".write": "auth != null",
        "games": {
            "$gameId": {
                ".read": "auth != null",
                ".write": "auth != null"
            }
        },
        "status": {
            "$userId": {
                ".read": "auth != null",
                ".write": "$userId === auth.uid"
            }
        }
    }
}